# Independent Validation

Validate DEX structure and hashes. For a real restoration, require both dexdump and JADX to accept every output; missing tools are a recoverable blocking question. Fixture pass-through is explicitly `orchestration-only` and does not prove customer VMP restoration.
