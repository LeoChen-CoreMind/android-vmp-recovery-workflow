# DEX Restore

Write only validated code units. Check source units, unchanged length, method fields, ULEB128 space, references, SHA-1, and Adler32. Require a repair manifest covering every VMP method recorded during DEX ingest. Block instead of silently rebuilding unsupported layouts.
