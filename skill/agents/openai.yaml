interface:
  display_name: "Android DexVMP Recovery Workflow"
  short_description: "Recover DexVMP and repack a version-adapted APK"
  default_prompt: "Use $android-vmp-recovery-workflow to process this APK and optional runtime DEX through recovery and validation, then perform the final version-specific 360 shell removal and signed APK repack stage."
