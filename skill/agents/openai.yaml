interface:
  display_name: "Android DexVMP Recovery Workflow"
  short_description: "Run resumable APK-to-DexVMP recovery cases"
  default_prompt: "Use $android-vmp-recovery-workflow to process this APK and optional runtime DEX through the checkpointed recovery workflow."
