interface:
  display_name: "Android DexVMP Recovery Workflow"
  short_description: "Orchestrate resumable SO, VM, simulation and DEX recovery stages"
  default_prompt: "Use $android-vmp-recovery-workflow to orchestrate an authorized Android DexVMP case with checkpoints, evidence hashes, and fail-closed validation."
