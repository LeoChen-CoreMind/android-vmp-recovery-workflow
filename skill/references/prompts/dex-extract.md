# DEX Extract

Run the unchanged static extractor and record its result. Import supplied runtime DEX with provenance when needed. Never label imported DEX as statically decrypted or VMP repaired. Block if no structurally valid DEX exists.
